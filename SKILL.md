---
name: yuwen-handout-to-courseware
description: 高中语文「讲义 docx → 课件 PPT」生成 skill（基于高一语文模板，第2讲古诗 case 跑通定稿）。输入讲义 docx（+学生版 PDF 核范围，可选标杆课件对照），输出整套课件 pptx：封面/目录/学习目标/考情/教材链接/典型例题/针对练习/内容总结/结束页，每小问"题目页→答案页"，选择题答案自动标红。当用户提到"语文讲义转课件"、"第N讲转PPT/出课件"、"高中语文课件"、"古诗/文言/小说/作文讲义做成PPT"、"按语文模板出课件"，或给出高中语文讲义要做演示文稿时，立即使用本 skill——即使没说"课件"二字。注意与 handout-to-courseware（小学数学版）区分：语文用这个。
---

# 高中语文 讲义 → 课件 PPT

核心定位：**讲义是题库与内容源，高一语文.pptx 模板是舞台，标杆课件是质量参照**。
skill 不创造教学内容，只做「提取 → 组装 → 渲染 → 自检」。

## 输入三件套（路径约定）

| 输入 | 默认位置 | 用途 |
|---|---|---|
| 讲义 docx | `~/Desktop/高中讲义&课件-语文/讲义/第N讲….docx` | 内容唯一来源 |
| 学生版讲义 PDF（同名 …学生版讲义-S班.pdf） | 同目录 | **范围裁决**：出处标签/题型标签/分值以它为准 |
| 模板 `高一语文.pptx` | `~/Desktop/高中讲义&课件-语文/讲义模板/` | 版式克隆源（22 样本页） |
| （可选）标杆课件 …-S班.pptx | 同讲义目录 | 逐页对照参照，仅部分讲次有 |

## 题型/讲次适配（已验证两类）

不同讲次的"阅读对象"和题型不同，content.json 用不同字段，脚本已支持：

| 讲次类型 | 阅读对象字段 | 渲染 | 题型 | 提取器 | 验证 case |
|---|---|---|---|---|---|
| **古诗**（炼字/鉴赏） | `poem`（短，1页） | 楷体居中、注释①②③上标、【注】仿宋 | 选择 / 简答赏析 | `extract_handout.py` | 第2讲古诗(70页) |
| **信息/现代文**（情境填空/论述类） | `material`（长，**自动分页**，右上"材料 n/m"） | **楷体·统一22pt·左对齐** | 选择 / **补写填空** / 简答 | `extract_handout_xinxi.py` | 第10讲信息(100页) |

- `build_pptx.py` 的 `_emit_passage` 按 content 里是 `poem` 还是 `material` 自动选渲染方式，无需改脚本。
- 信息类**没有"诗"，材料超长**（如"种植问答"1700字）→ `_paginate_material` 按段落分页；考情表常 4 列（古诗 5 列），`add_table` 列宽自动容错。
- **材料字体=楷体、字号统一 22pt（对齐标杆，不可每页 `_fit_size` 缩放，否则忽大忽小）**。统一字号下按容量分页：`_paginate_material(max_chars≈210)`（问答体短行多，取保守值），内容区起点固定→各页容量一致、不溢出。这是踩过的坑：先按字数切页再每页独立缩字号，会导致同一份材料各页字号不一。
- **问答体材料（如"种植问答"问一/答…问十五/答）分页时把"答…"段绑定到前面的"问…"段成一个不可拆的块，让同一个问答对落在同一页**（否则问在上页末、答在下页首，阅读割裂）。块不跨页，单块超容量才按句切。
- **补写题**是"长题干（带横线文段）+ 短答案"：简答答案页用"题干高度按内容自适应、答案取剩余且保底 0.9in"布局（避免长题干把答案挤成负高度——信息类踩过）。
- 文言/小说/作文等其他讲次：若阅读对象是长文段就用 `material`，短引文用 `poem`；题型沿用 choice/essay。

## 五步流程

```
① 提取   python3 {SKILL_DIR}/scripts/extract_handout.py --docx 讲义.docx --merge content.json
② 填内容  按 references/content_schema.md 补 content.json（诗/题/出处标签，逐字取自讲义）
③ 渲染   python3 {SKILL_DIR}/scripts/build_pptx.py --content content.json \
            --template 高一语文.pptx -o 第N讲_课件.pptx
④ 自检   python3 {SKILL_DIR}/scripts/check_overflow.py 第N讲_课件.pptx   # 必须双0
         + soffice 转 PDF → 全页缩略网格逐页视觉扫（详见下文三道防线）
⑤ 交付   提醒用户：预览字体会 fallback，真实效果在 WPS/PowerPoint 看
```

新讲次从 `references/content_example_第2讲古诗.json` 复制起步最快。
首次接触模板或换模板（高二高三语文.pptx）时，先读 `references/template_map.md`。

## 内容范围：差异分类法（必须执行）

和标杆/讲义逐项对照，每个内容归三类（这是甲方认可的裁决规则）：

- **A 讲义有 → 做**：学习目标表、考情表、教材链接、典型例题、针对练习全部题（诗+题干+答案）、解题步骤。
- **B 讲义没有、老师授课时另加 → 不做**：入门测/出门测、学期课程总表、重点巩固（标杆里有但讲义无的题）、题目难度★。判断依据=学生版 PDF 里有没有。
- **C 模板没有的文字、但讲义有 → 做，且参考标杆样式呈现**：题/诗/答页顶部模块标题气泡、诗居中、出处/题型标签、目录序号样式。
- 讲义的「知识方法」整章（炼字概述/词类表等）与每题【解析】：**默认不做成页**（标杆没有；
  提取器已抽出存在 content.json，用户要时一键加回）。

## 课件结构语法（定稿）

```
封面 → 目录(5模块·圆角方块序号)
模块一 学习目标：分隔页 + 学习目标表 + 难度星级说明
模块二 考情分析：分隔页 + 考情表
模块三 教材链接：分隔页 + 正文
模块四 典型例题：分隔页 + 诗页 + 必做(题→答) + 选做(题→答)
模块五 针对练习：分隔页 + 每练习[诗页 + 每小问(题→答)]
内容总结(解题步骤) → 结束页
```
- 每首诗 1 页（居中，注释序号①②③右上角上标，【注】行仿宋）。
- 每小问 = 空白题目页 + 答案页两张（讲课先看题再公布答案）。
- 选择题答案页：括号填字母（**红色加粗**）+ **正确选项整行标红**，其余选项黑。
- 简答答案页：短题干 + 答案**宋体全红**无背景框。
- 【课前/中/后测】标签只标在该练习**第 1 小问**。

## 字体 / 颜色（甲方定稿，改前必须确认）

速记：**古诗楷体 · 注释仿宋 · 题目黑体 · 答案宋体全红 · 标签标题微软雅黑 · 封面目录结束继承模板**。
完整表 + 颜色值 + 为什么这样定（含两次返工教训）→ `references/font_color_spec.md`。

涉及字体的任何争议：先用脚本逐元素提取对象 PPT 的 `<a:ea typeface>` 列成分布表给用户看，
**拿事实对齐再改代码**——别按印象猜（"看着像楷体"实测可能是黑体）。

## 防出框三道防线（每次出稿必须全过）

出框 = 文字超出文本框/页面，是甲方最高频投诉。三道防线缺一不可：

1. **渲染时**：所有内容框（诗/题干/答案/正文/出处）走 `_fit_size()` 自动缩字号。
   关键：**估算行距必须与实际渲染行距一致**（诗1.4/题干1.3/答案1.25/正文1.5），
   字宽系数 1.1 保守；出处标签按长度缩号保证一行。
2. **生成后**：跑 `scripts/check_overflow.py`（文字溢出严格估算 + shape 边界），**双 0 才算过**。
3. **视觉**：`soffice --headless --convert-to pdf` → pymupdf 渲染全页 → PIL 拼缩略网格
   （5×4/张）→ 逐页扫一遍；长内容页（最长诗/最长答案/4选项长题）放大复核。

## 生成前全面自检清单

- [ ] 内容齐全：题目小问数、诗歌数与讲义一致；模块顺序=结构语法；slide_structure.json 落盘可审
- [ ] 字体/字号/颜色：抽 代表页逐 run 核 `ea typeface` + color 与规范表一致
- [ ] 出框：三道防线全过
- [ ] 选择题：每题括号已填字母且红、正确选项行红
- [ ] 克隆完整性：逐 pic 读 image.blob 无异常（关系重映射没漏）

## 踩坑速查（都是真实翻过的车）

| 现象 | 根因 | 修法 |
|---|---|---|
| 克隆页图片全裂 | deepcopy 后 r:embed 指向源页 rId | clone_slide 必须重映射关系（脚本已内置） |
| 页面叠着"空间几何体"/数学题 | 模板示例**贴图**在 slide 上被克隆带过来 | 按 template_map.md 删指定 Image/组合 |
| 长诗/长答案溢出框 | _fit_size 估算行距 ≠ 实际行距 | 两者对齐 + check_overflow 复核 |
| 出处标签换行出框 | 28 字标签在 0.5in 框 | 按长度缩字号保一行 |
| 字体改了用户仍说不对 | 按主观印象统一字体 | 逐元素提取实测 ea 列表对齐，让用户拍板 |
| 标题"内容总结"等换行 | 模板标题框窄 | 清原框、自绘宽标题框 |
| 生僻字乱码（𮭨） | 字体无字形回退 | 换常用字形（鷃） |
| LibreOffice 预览字体不对 | 本机缺中文字体 fallback | 预览只看排版颜色；字体以 XML ea + WPS 实看为准 |

## 与其他 skill 的关系

- `handout-to-courseware`：**小学数学**版（教学设计稿驱动、另一套模板）。语文用本 skill。
- `ppt-new-template`：换新模板时先用它注册版式，再更新本 skill 的 `template_map.md` 与 `T` 映射。
- `ppt-script-fix`：渲染后排版疑难杂症的测试修复循环。
